apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: cloudflare-tunnel-endpoint
spec:
  endpoints:
    - dnsName: "external.{{ .Values.domainName }}"
      recordType: CNAME
      targets:
        - "{{ .Values.tunnelId }}.cfargotunnel.com"
