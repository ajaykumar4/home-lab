apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

configMapGenerator:
- name: cloudflare-secret-template
  files:
    - values.yaml

resources:
  - ./cloudflare-secrets.yaml

generatorOptions:
  disableNameSuffixHash: true
