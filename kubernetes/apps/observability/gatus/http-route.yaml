apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: gatus-http-route
spec:
  parentRefs:
    - name: external
      namespace: kube-system
  hostnames: ["status.aknuk.dev"]
  rules:
    - backendRefs:
        - name: gatus
          port: 80
