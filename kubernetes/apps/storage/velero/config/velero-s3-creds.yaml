apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: velero-s3-creds
spec:
  refreshInterval: "1h0m0s"
  target:
    name: velero-s3-creds
    template:
      engineVersion: v2
      data:
        cloud: |
          [default]
          aws_access_key_id={{ .username }}
          aws_secret_access_key={{ .password }}
  secretStoreRef:
    name: passbolt
    kind: ClusterSecretStore
  data:
    - secretKey: username
      remoteRef:
        key: 038783b6-400d-4ad3-b050-3634c14cb78d
        property: username
    - secretKey: password
      remoteRef:
        key: 038783b6-400d-4ad3-b050-3634c14cb78d
        property: password
