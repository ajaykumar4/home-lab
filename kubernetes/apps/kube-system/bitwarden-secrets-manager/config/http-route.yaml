apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: bitwarden-http-route
  namespace: argo-system
spec:
  parentRefs:
    - name: external
      namespace: kube-system
  hostnames: ["bitwarden.aknuk.dev"]
  rules:
    - backendRefs:
        - name: bitwarden-sdk-server
          port: 9998
