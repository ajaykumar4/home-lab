apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: passbolt
spec:
  provider:
    passbolt:
      auth:
        passwordSecretRef:
          name: passbolt-creds
          key: password
          namespace: kube-system
        privateKeySecretRef:
          name: passbolt-creds
          key: privateKey
          namespace: kube-system
      host: https://passbolt.aknuk.dev
