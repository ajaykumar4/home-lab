apiVersion: viaduct.ai/v1
kind: ksops
metadata:
  name: clustersecretstore-secret
  annotations:
    config.kubernetes.io/function: |
      exec:
        path: ksops
files:
  - ./clustersecretstore.sops.yaml
