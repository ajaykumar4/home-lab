node-feature-discovery:
  enabled: true

crds:
  defaultCR:
    install: true

deviceConfig:
  spec:
    devicePlugin:
      devicePluginImage: rocm/k8s-device-plugin:5.7.1
      # To specify tolerations for nodes with AMD GPU taints that are unrelated to ROCm:
      # devicePluginTolerations: []
